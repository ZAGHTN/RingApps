/**
 * Application Verifier le type d'une variable
 * Autheur     : Zaghdoudi Chokri
 * Date        : 22/08/2020
*/
# =====================================
# Structure (بلأولوية: تركيب البرنامج)
# Statements (Commandes, load etct)
# Fonctions
# Packages & Classes

myString = "test"
myNumber = 7
myList   = [1,2,3]
? type(myString) ? type(myNumber) ? type(myList)
? ""
? isstring(myString) ? isnumber(myNumber) ? islist(myList)
? ""
? isstring(myString) ? isstring(myNumber) ? islist(myNumber)
